# Claude 外部评审 — CLAUDE.md 精简方案

## 评审结论

**通过，但需补一个兜底机制。** 砍掉的内容可以砍，但需要确保 AI 在需要时能自己找到子项目的 CLAUDE.md。当前方案砍掉了所有"见 xx/CLAUDE.md"的提示。

---

## ✅ 思路对齐

- 364 → 180 的目标合理，CLAUDE.md 确实太长了
- 砍掉的四块（部署架构、后端命令、前端构建、沟通示例）在子项目 CLAUDE.md 中都有
- 保留的核心工作流区块（三规则 + orca-review + /verify + 门控）是 AI 每次对话必用的

## ⚠️ 遗漏或风险

1. **Project Overview 中删掉子项目引用可能导致 AI 找不到上下文。** 当前 CLAUDE.md 明确写了"详见 jeecg-boot/CLAUDE.md"和"详见 jeecgboot-vue3/CLAUDE.md"。全砍掉后端/前端章节后，AI 可能不知道去哪找编译命令和构建配置。建议在 Project Overview 保留一行指向子 CLAUDE.md 的链接。

2. **沟通风格压缩到 5 行可能丢掉 Bash 描述规则。** 当前沟通风格区有一条关键约束：Bash 命令的描述必须用中文业务语言。如果压缩时删了这条，AI 提交时的 Bash 描述会变成技术黑话（业务人员看不懂权限弹窗）。建议 5 行中保留这条。

3. **Quick Start 放在哪？** Docker 启动命令砍掉没问题（不常用），但 Prerequisites 保留合理。

## 💡 优化建议

- 步骤清单表（12行）可以考虑简化：只保留阶段和命令两列，去掉轻量/标准/全量的勾选矩阵。分级测试表已经覆盖了触发条件，步骤清单的勾号是冗余信息。

- Super Harness 规则索引 23 条可以不加序号，用紧凑列表节省约 4 行。

---

*评审人：Claude（降级手工评审）*
*日期：2026-07-24*
