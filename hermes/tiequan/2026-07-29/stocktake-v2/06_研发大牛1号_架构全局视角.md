# 06_研发大牛1号_架构全局视角

> 审计对象：MES 盘点单 V2
> 审计日期：2026-07-29

## 新增接口 refreshItems ✅
```java
@Transactional(rollbackFor = Exception.class)
public void refreshItems(String id) {
    MesStocktake exist = baseMapper.selectByIdForUpdate(id);   // FOR UPDATE 锁
    if (!"1".equals(exist.getStatus())) throw ...;              // 草稿守卫
    List<MesStocktakeItem> items = snapshotItems(warehouseId);
    itemMapper.delete(delQw);                                   // 删旧行
    calcAndSaveItems(exist, items);                             // 全量重插
    baseMapper.updateById(new MesStocktake().setId(id).setSnapshotTime(...));
}
```
- 锁 + 状态守卫 ✅
- 事务边界正确（单个事务）✅
- **但 snapshotTime 更新用了 `updateById` 空 entity**（见前轮审计 P2-4）🟡

## validateBookQty 架构 🔴 P1
```java
// 编辑原有行：读 oldBookMap（普通 SELECT 无锁）→ 比较
// 新行：FOR UPDATE 逐物料加行锁 → 比较
```
- `oldBookMap` 用普通 SELECT 在**事务外**构建（`updateWithItems` 事务开启前读）→ 读到的 book_qty 可能已过期 🔴 P1
- `FOR UPDATE` 在 `updateWithItems` 事务内 → 锁持有时间=整个事务 ✅

## 批量审核架构 🔴 P0
逐条 `auditStocktake` API 调用 → 每个调用**独立事务**（非同一 Spring 事务上下文）→ 非原子性。同边界2号。🔴 P0

## JeecgBoot 规范
- @RequiresPermissions 覆盖 ✅（refreshItems 复用 edit 权限）
- update-begin/end 完整 ✅
- @Transactional rollbackFor ✅
- Spring 代理调用 ✅（无 this.xxx() 反模式）

## 结论
1 P0（批量审核非原子）、1 P1（oldBookMap 无锁）、1 P2（updateById 空 entity）。架构主体扎实。
