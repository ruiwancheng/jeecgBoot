# 01_风控总报告 — MES 盘点单 V2 审计

> 审计日期：2026-07-29 | 审计对象：`mes/stock/stocktake` V2（黄金模板重构版）
> 审计范围：4e4fff7..450c490 | 10 Agent 并行

---

## 🔴 共识 P0（6 项）

| # | 问题 | Agent | 说明 |
|---|------|-------|------|
| **P0-1** | **refreshItems 覆盖用户已填实盘数** | 产品2号, 研发2号, 研发1号 | `snapshotItems` 设 `actualQty=bookQty`（刷新后 actual=账面，差异=0）。用户填 120 后刷新→丢失。修复：保留已有 actualQty 按 materialId 匹配旧行。 |
| **P0-2** | **validateBookQty 缺 unitCost 校验** | 产品3号, 测试4号, 测试2号 | `validateBookQty` 只校验 `bookQty` 数量一致性，未校验 `unitCost` 等于当前 `moving_avg_cost`。API 可传任意 unitCost→差异金额被放大→成本污染。 |
| **P0-3** | **批量审核非事务性** | 产品2号, 研发1号, 研发2号 | 前端逐条 `auditStocktake` 独立事务→前 3 条成功第 4 条失败→前 3 条已审核（无反审核回退）。无错误提示。 |
| **P0-4** | **refreshItems 覆盖手工成本** | 产品3号, 测试4号 | 刷新后 `unitCost` 重取系统移动平均→手工修改的成本丢失。 |
| **P0-5** | **前端批量添加/展开行/loadMaterialMap N+1 查询** | 测试3号, 产品2号, 研发1号 | 3 个明显 N+1 点：`onBatchMaterials` 逐行 API、`StocktakeItemsSubTable` 逐物料 API、`loadMaterialMap` 逐物料 API。 |
| **P0-6** | **oldBookMap 无锁读取** | 研发1号, 研发3号 | `updateWithItems` 中 `oldBookMap` 用普通 SELECT 在事务外构建→读到值可能已过期→编辑时 `validateBookQty` 校验可能通过/失败都误导。 |

## 🟠 共识 P1（4 项）

| # | 问题 | Agent |
|---|------|-------|
| P1-1 | 零库存盘盈 `unitCost=0`→跳过成本更新→业财偏差 | 产品2号, 产品3号, 测试4号 |
| P1-2 | 批量添加物料 race（前端逐行拉账面 + 后端 FOR UPDATE 校验）| 研发3号, 产品2号 |
| P1-3 | `create_time` 索引仍缺失 | 测试3号 |
| P1-4 | refreshItems→用户编辑冲突（刷新与编辑无锁协调）| 研发3号 |

## 🟡 共识 P2（4 项）

| # | 问题 |
|---|------|
| P2-1 | 无反审核 |
| P2-2 | refreshItems 无"将覆盖已填实盘数"确认 |
| P2-3 | 展开行调整单无点击跳转 |
| P2-4 | `updateById` 空 entity 方式不清晰 |

## 上轮 P0 修复复核：4/4 ✅

## 修复优先级
```
Phase 1（本周）:
  P0-1: refreshItems 保留 actualQty（按 matId 匹配）
  P0-2: validateBookQty 加 unitCost 校验
  P0-6: oldBookMap 加 FOR UPDATE（或在事务内读）

Phase 2（下周）:
  P0-3: 批量审核改为后端 batchAudit（单事务）
  P0-5: 后端批量 query material/inventory（一次 SQL）
  P1: 零库存盘盈成本警告/refresh UI 确认

Phase 3: P2 各项
```
