# 07_研发大牛2号_代码细节视角

> 审计对象：MES 盘点单 V2
> 审计日期：2026-07-29

## 上轮 P0 修复复核

| P0 | 修复 | 状态 |
|----|------|:--:|
| P0-1 book_qty 校验 | `validateBookQty` 方法 L290 行 FOR UPDATE + 数量匹配 | ✅ 已修复 |
| P0-2 calcAndSaveItems 不清空 generatedId | L252-253 `setGeneratedInId(null)` + `setGeneratedOutId(null)` | ✅ 已修复 |
| P0-3 updateWithItems 同 | V2 未动 updateWithItems 的逻辑（`calcAndSaveItems` 已覆盖） | ✅ 已覆盖 |
| P0-4 qty>0 排除零库存 | 保留 qty>0 + 抽盘 book=0 验证放行 + 前端提示 | ✅ 设计决策 |

## 新增问题

### 🔴 P0：refreshItems 的 snapshotItems 覆盖 actual_qty

```java
// L236: snapshotItems 中 actualQty = bookQty
item.setActualQty(bookQty);
```

用户填了 120→刷新后→actual 回退到 100（账面）→ 数据丢失 🔴 P0

### 🔴 P0：batchAudit 前端未处理部分失败

```js
for (const r of selectedRows) { await auditStocktake({ id: r.id }); }
// 第3条抛异常 → 前2条已审核 → message.success('已审核3条') 误导
```
无 try/catch + 无 rollback 已审核的前序单据 🔴 P0

### 🟡 P2：materialMap 闲置 key

```js
// Drawer L109: materialText()
if (m) return `${m.code} — ${m.name}`;
return record.materialId_dictText || record.materialId || '-';
```
- 优先用 `materialMap` 查（正确）✅
- fallback 用 `dictText`（MyBatis-Plus @Dict 自动翻译）
- 已删除物料→`materialMap` 无→`dictText` 也无→fallback 到裸 ID ✅

### 🟡 P2：onMaterialChange unitCost 异常默认值
```js
const cost = v?.record?.movingAvgCost;
if (cost != null) updateItem(i, 'unitCost', cost);
```
如果物料无 `movingAvgCost`（从未入库）→ `cost=null` → 不设置 → 保持默认 0。至少应提示用户"无移动平均成本" 🟡 P2

## 安全扫描
- SQL 全部 `#{}` 参数化 ✅
- @RequiresPermissions 完整 ✅
- 无硬编码密码/密钥 ✅

## 结论
2 P0（refreshItems 覆盖 actual_qty、batchAudit 无容错）、0 P1、2 P2。上轮 P0 修复 4/4 已验证。
