# 06_研发大牛1号_架构全局视角

**审计对象：** MES 编码规则模块（codeRule）+ 10 单据取号接线
**视角：** 事务边界、服务分层、性能隐患、JeecgBoot 规范
**执行说明：** 降级模式（总调度单体会审），非独立 Agent

## 结论
分层标准（Controller→Service→Mapper）、权限注解、菜单注册均合规；映射常量 bizCodeMap 仅前端单点维护，后端无对应枚举，属于可接受的轻量取舍。

## Findings

| 级别 | 位置 | 问题 | 影响 | 建议 |
|---|---|---|---|---|
| P2 | bizCodeMap.ts（前端唯一映射点） | 单据↔规则映射只存在于前端常量，后端无校验：前端传错 ruleCode 也照取 | 配错时编码张冠李戴 | 中期可在规则表加唯一约束 biz_type UNIQUE，从数据层防"一单据多规则" |
| P3 | MesCodeRuleServiceImpl | `@Slf4j` 的 log 在删除自动创建逻辑后已无使用点 | 编译告警级 | 下次顺手清理 |
| — | Controller | 继承 JeecgController、@RequiresPermissions 覆盖 list/add/edit/delete/deleteBatch/export | 合规 | — |
| — | 菜单注册 | MesMenuRegistry leaf + addPerms 注册 7 个权限点，Runner 自修复机制兜底 | 合规 | — |
| — | 事务 | nextCode 有 @Transactional；CRUD 简单操作走 MyBatis-Plus 默认 | 合规（事务内并发现问题见 08 报告） | — |
