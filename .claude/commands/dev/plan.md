---
description: 自有命令 — 制定实施计划：查模块、锚定模式、列文件
---

# /plan <任务描述>

需求确认后制定实施计划。

**铁律：禁止"TODO"、"TBD"、"适当处理"。每步必须指定具体文件和操作。**

## 流程

### 1. 加载领域知识
使用 `plan` 技能获取：features.json 结构说明、标品概念、三种策略判断标准（纯新增/覆盖标品/加字段）。

### 2. 查模块
查 features.json 找到相关标品模块位置。

### 3. 模式锚定
读标品类似代码，引用具体文件和行号作为参照。

### 4. 判断策略
按技能中的三种策略标准判断。

### 4.5. orca-review（delegate 模式强制，直接模式免评）

- **delegate 模式：** worker preamble 自动强制执行，此处不用重复判断
- **直接模式（仅限文案/注释/样式）：** 免评，直接进入步骤 5

如果 Orca 不可用，标注"降级：手工评审"并至少逐个对照依赖查证清单复查。

### 5. 输出计划
文件清单 + 任务步骤（含具体代码和验证命令）+ 范围外 + 风险。

### 6. 用户确认后执行
