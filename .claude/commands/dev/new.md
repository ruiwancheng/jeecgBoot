---
description: 自有命令 — 开启新会话：在当前终端上下文膨胀时，一键生成记忆卡片 + 打开新 pi 终端作为控制中心
---

# /new

上下文膨胀时，打开一个全新的 pi 终端作为控制中心——带齐所有规则和状态，但没有历史噪音。

## 与 /delegate 的区别

| | /delegate | /new |
|------|:--:|:--:|
| 用途 | 派一个具体任务给工人 | 开一个干净的指挥部 |
| 终端 | 工人干完自动销毁 | 长期存活 |
| 卡片 | 卡片+工作流+任务 | 卡片+工作流+提示"这是新控制中心" |

## 流程

1. 运行 `/cleanup-context` 生成记忆卡片（含会话上下文——最重要的增量信息）
2. 用 `orca terminal create --command "pi"` 创建新 pi 终端
3. 等待终端就绪（`orca terminal wait --for tui-idle`）
4. 用 `orca orchestration task-create` + `dispatch --inject` 把记忆卡片注入新终端
   - 和 `/delegate` 同样的 `--inject` 机制，卡片作为初始上下文注入，而非普通用户消息
5. 提示用户："新控制中心已就绪，切换到那个终端即可开始工作"

## 注入内容格式（task spec）

```json
{
  "title": "新控制中心",
  "description": "## 🎯 你是 MES 项目的新控制中心\n\n上下文已重置，无历史噪音。请先消化以下记忆卡片了解项目状态和当前会话上下文。\n\n<记忆卡片——含当前会话上下文+关键提醒+项目状态>\n\n---\n\n消化完后回复ready，等待用户指令。不要主动做任何事。"
}
```

## 使用示例

```
用户（在当前膨胀终端里）：/new
AI：📋 生成记忆卡片... ✅
    🚀 创建新终端 term_xxx ✅
    📤 --inject 注入记忆卡片 ✅
    
    ✅ 新控制中心已就绪：切换到 term_xxx 继续工作
    （当前终端可以关闭了）
```

## 降级

如果 Orca 不可用：退化为 `/cleanup-context` + 提示用户手动开终端粘贴记忆卡片。
