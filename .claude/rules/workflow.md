---
name: workflow
description: 开发流程——需求→计划→实现→自验证→分级测试→提交
glob: "**/*"
version: 2.1
---

# 开发流程

```
/brainstorm → /plan → 用户确认 → 写代码 → /verify → git commit + push → 部署 → 分级测试 → /done → 人工验收
```

> /done 完成后，AI 提示用户可选择性调用铁拳团审计和鹰眼团测试做兜底检查，详见文末。

## 防失忆触发条件（强制）

**每完成一步，必须在回复末尾提示下一步是什么。** 具体触发规则：

| 当 AI 刚刚完成了 | 必须做 | 触发词 |
|------|------|------|
| 需求讨论（/brainstorm 结束） | 提示"确认无误就进入 /plan" | — |
| 用户说"创建/新增XX模块/管理" | **制动 `/new-module`**（不手动输入也触），输出模块清单后再进入 /plan | 匹配"创建/新增/新建模块/管理" |
| 实施计划（/plan 结束） | 提示"确认计划就进入写代码"，**同步输出依赖查证清单** | — |
| 写代码完成（最后一批文件写入后） | **自动进入 /verify**（不要等用户说） | git diff 有变更 + 本次写入过文件 |
| /verify 通过 | **自动 commit + push，然后提示部署** | git diff 有变更 |
| **部署完成** | **自动 git diff 差集 → 影响模块 → 精准回归 → 更新 .last-deploy-commit** | git log .last-deploy-commit..HEAD |
| 部署完成 | 提示进入分级测试 | — |
| 测试全部通过 | 提示进入 /done | — |
| 测试有失败 | 进入 /debug，修复后重测 | 报错信息 |
| **多轮改动累积（新增-2026-07-22）** | **暂缓部署，一次性集中部署** | 本地后端可用时（/start 启动），多轮修复在本地逐轮 mvn compile+curl 验证；全部就绪后 push + 一次部署 |

> ⚠️ **最常见失忆点：写完代码直接结束，跳过了 /verify。**
>
> ⚠️ **第二失忆点：/verify 通过后直接让用户部署，代码没提交推送。** 服务端走 `git pull`，未推送则部署白跑。
>
> ⚠️ **第四失忆点：本地后端可用时，每轮改动都部署。** `/start` 启动本地后端后，Java 改动 5 秒生效——等所有改动就绪后一次性部署，不要每修一行都部署一次。
>
> ⚠️ **第五失忆点：部署后凭记忆回归，遗漏变更模块。** 部署后 AI 必须跑 `git log .last-deploy-commit..HEAD --name-only` 列出全部变更→精准回归，验证完更新 `.last-deploy-commit=HEAD`。

## 会话分段规则

| 大任务定义 | 示例 |
|-----------|------|
| 新建模块（含完整 CRUD） | 发货单/供应商/物料模块 |
| 审计修复（≥5 个文件变更） | 铁拳团 P0 修复 |
| 大规模重构 | PRD 对齐/四级→两级回退 |

**触发条件：** 当前会话已完成 ≥2 个大任务时，AI 必须在当前任务 `/done` 后主动提示：

```
本会话已完成 2 个大任务（A、B），建议 /session-wrap 收尾后开新会话继续，避免上下文膨胀导致 Token 浪费。
```

**例外：** 连锁修复不算大任务（如部署后发现权限问题→补 SQL→重部署，属于同一个大任务的延续）。

## 分级测试规则（AI 自动判定）

写代码后 AI 分析 `git diff`，按变更影响面自动选择测试级别：

### 轻量级（微小改动）
- 只改了 Vue 模板文字/样式、注释、`.data.ts` 列宽/标签、单个字段校验规则
→ 只跑 `/verify`，跳过回归测试

### 标准级（日常改动）
- 新增/修改 Controller 方法、Vue 组件逻辑、Service 方法、`.data.ts` Schema、≤3 个文件
→ `/verify` + `/test-api <模块>`

### 全量级（高风险改动）
- 新增/修改 Entity 字段、新增 Controller、修改 Mapper/XML/SQL、修改 pom.xml、≥5 个文件
→ `/verify` + `/test-api` + `/test-e2e` + `/test-all`

### 不变更不测试
没有 `git diff` → 全部跳过

## 步骤清单

| 步骤 | 命令 | 轻量 | 标准 | 全量 |
|------|------|:---:|:---:|:---:|
| 需求澄清 | /brainstorm | ✓ | ✓ | ✓ |
| 实施计划 | /plan | ✓ | ✓ | ✓ |
| 编码实现 | - | ✓ | ✓ | ✓ |
| 自验证 | /verify | ✓ | ✓ | ✓ |
| 提交推送 | git commit + push | ✓ | ✓ | ✓ |
| 部署 | 部署控制台 | ✓ | ✓ | ✓ |
| 前端静态 | /test-frontend | ✓ | ✓ | ✓ |
| 模块测试 | /test-api | - | ✓ | ✓ |
| E2E 测试 | /test-e2e | - | - | ✓ |
| 全量测试 | /test-all | - | - | ✓ |
| 完成检查 | /done | ✓ | ✓ | ✓ |

遇报错用 /debug，修复后自动调 /verify。

## PRD 阅读规则（/brainstorm 阶段强制）

**PRD 核心逻辑和操作演示必须同时阅读，不可只看其一。** 操作演示里的交互动词决定数据结构：

| 操作演示动词 | 数据结构含义 |
|-------------|-------------|
| "选择/关联 XX" | 外键引用（如 deliveryNoteId） |
| "系统自动带出产品、数量" | 明细行 + 关联查询（主子表） |
| "输入/填写实际数量" | 明细行独立字段（如 actualQty） |
| "多行/明细/逐行" | 主子表而非单表 |

> **来源模块：** 销售出库。单看核心逻辑判断为单表，补读操作演示后发现应是主子表，多花一倍工作量。

## 开发前依赖查证（/plan 阶段输出）

新模块开发前，AI 必须逐项确认以下依赖链可用，把坑提前排除：

| # | 检查项 | 验证方法 |
|---|--------|---------|
| 1 | Shiro 权限链 | 确认菜单+权限码在 `MesMenuRegistry` 注册，Runner 自修复机制会补 `perms` |
| 2 | SQL 兼容性 | 新 SQL 文件不含 `IF EXISTS`、`sys_dict_item` 不含 `del_flag` |
| 3 | 前端组件 | 表字典用 `JSearchSelect`（合写 `dict: 'table,text,code'`），不用 `a-form-item` |
| 4 | 字典存在 | 需要的 `sys_dict` + `sys_dict_item` 已在 SQL 中注册（DELETE+INSERT 幂等） |
| 5 | 父菜单存在 | 新模块的 `parentId` 指向的菜单已注册，检查 `MesMenuRegistry` |

> **为什么：** 权限/perms、SQL部署、表单渲染这三个坑每次新模块都踩，Token 消耗最大。

## 推送前检查（每次 git push 前自动执行）

| 检查项 | 阻塞？ | 说明 |
|--------|:--:|------|
| boot-module 声明的模块目录存在 | 是 | 缺目录 → 编译失败 |
| system-start 有对应依赖声明 | 是 | 缺依赖 → 运行时找不到类 |
| 模块代码已 git 跟踪 | 是 | 漏提交 → CI 编译失败 |

## 可选兜底检查（/done 后提示）

/done 完成后，AI 提示用户可选择性执行以下兜底检查：

```
日常开发流程已完成。如需深度质量检查，可选择：
1. 铁拳团审计 — 本地10人AI代码审计，发现隐藏风险
2. 鹰眼团测试 — gen-tests + test-api + test-e2e 全面回归
```

### 铁拳团审计说明

10 人 AI 并行审计（3 产品 + 3 研发 + 4 测试），共识 P0 必改。适用于新增模块、重大重构场景。

| 场景 | 建议 |
|------|------|
| 新增模块（新 Entity + Controller） | 强烈建议审计 |
| 新增/修改 Entity 字段 | 建议审计 |
| 修改 Service 核心逻辑 | 可选审计 |
| Bug 修复或样式调整 | 不需要 |

### 鹰眼团测试说明

gen-tests 从 Controller 自动推导测试用例 + 自定义规则（NPE/Auth/SQL注入/唯一/溢出/并发），test-api + test-e2e 回归验证。

## 质量门控编排管道（Orca 增强，可选）

当 Orca orchestration 可用时，verify → review → test 三阶段可升级为编排 DAG：

```
git commit + push
     │
     ▼
┌─────────────┐   失败    ┌──────────┐
│ verify 门控  │─────────>│ /debug    │
│ (curl+page)  │          │ 修复后重入 │
└──────┬──────┘          └──────────┘
       │ 通过
       ▼
┌─────────────┐   CRITICAL ┌──────────┐
│ review 门控  │──────────>│ 阻断提交   │
│ (7维审查)     │           └──────────┘
└──────┬──────┘
       │ 通过 (无 CRITICAL)
       ▼
┌─────────────┐   失败    ┌──────────┐
│ test 门控    │─────────>│ /debug    │
│ (APIs+E2E)   │          │ 修复后重入 │
└──────┬──────┘          └──────────┘
       │ 通过
       ▼
    /done 信号
```

**决策规则：**

| 门控 | 通过条件 | 失败动作 |
|------|---------|---------|
| verify | 所有变更类型验证通过 | 进入 /debug，修复后重新入编排 |
| review | 无 CRITICAL / HIGH 问题 | CRITICAL → 阻断提交；HIGH → 标记但允许继续 |
| test | API 通过率 ≥95%, E2E 通过率 ≥90% | 进入 /debug，最多重试 2 次 |

> 降级策略：Orca 不可用时 → 使用标准线性工作流（/verify → /review → /test-all → /done）。编排管道是加速路径，不影响标准流程。
