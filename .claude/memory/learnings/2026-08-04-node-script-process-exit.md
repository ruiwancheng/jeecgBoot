[2026-08-04] [test] Node 脚本 process.exit 杀死父进程 | 触发：Node 链路口文件 `run().then(ok => process.exit(0))` | 处理：区分"独立运行"和"被 require 调用"——用 `if (require.main === module) { process.exit(...) } else { module.exports = { run } }`。本案：purchase-chain 段文件 `process.exit(0)` 立即终止 Node，require 的段 2/段 3 永远不跑——CI 一直显示绿但实际只跑 1/3。结论：**Node 链路口默认导出 run 函数**（不是立即 exit），编排器 `await require(...).run()` 顺序执行。
