# 本地开发环境 — 后端直连服务端数据库/Redis（Mac → Tailscale → 100.122.125.106）
# 使用方式：mvn spring-boot:run -Dspring-boot.run.profiles=local
spring:
  datasource:
    dynamic:
      datasource:
        master:
          url: jdbc:mysql://100.122.125.106:3306/jeecg-boot?characterEncoding=UTF-8&useUnicode=true&useSSL=false&tinyInt1isBit=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
          username: root
          password: root
  data:
    redis:
      host: 100.122.125.106
      port: 6379
      password: ""
