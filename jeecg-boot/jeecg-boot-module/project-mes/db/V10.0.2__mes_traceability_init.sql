-- ============================================================
-- V10.0.2  MES批次追溯（traceability）：切片1占位
-- 作者:ruiwancheng  日期:2026-08-03
--
-- 本切片（trace-1-list）只实现批次追溯的列表查询后端，
-- 不新建独立表，复用 V8.0.0 已建的 c_mes_batch_ledger（批次流水表）。
-- 追溯视图通过 MesBatchTraceability 实体（@TableName="c_mes_batch_ledger"）
-- 暴露，未来扩展追溯专有字段（如追溯链层级）时再考虑新建独立表。
--
-- 后续切片预留：
--   trace-2-detail：按 biz_type+biz_id 反查上游单据 → 可能需要新增追溯链缓存表
--   trace-3-export：批量导出 → 复用当前列表视图，可能新增导出字段
--
-- 当前 SQL 文件仅作版本锚点 + 设计意图留痕，无 DDL/DML 执行。
-- ============================================================

SELECT 1;